@model IQueryable<ServiceItem>
    <section>
        @if (Model?.Any() == true)
        {
            int i = 0;
            <section>
                <header class="major">
                    <h2>Останні товари</h2>
                </header>
                <ul class="small-image-list">
                    @foreach (ServiceItem entity in Model.OrderByDescending(m => m.DateAdded))
                    {
                        <li>
                            <a asp-area="" asp-controller="Services" asp-action="Index" asp-route-id="@entity.Id">
                                <img src="~/images/@entity.TitleImagePath" alt="" class="left" style="width: 200px; height:200px;"/>
                            </a>
                            <h4>
                                @entity.Title
                            </h4>
                            <p>
                                Категорія: @entity.CategoryItem, Магазин: @entity.ShopItem, ціна: @entity.Price
                            </p>
                        </li>
                        i++;
                        @if (i == 3) break;
                    }
                </ul>
            </section>
        }
    </section>